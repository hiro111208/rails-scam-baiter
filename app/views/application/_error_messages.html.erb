<!-- This helper is used to display error messages when there is a problem with creating or editing a user, such as missing first name which is necessary for user creation, then press submit button. -->

<% if object && object.errors.size > 0 %>
  <div id="error-explanation">
    <h2><%= pluralize(object.errors.size, "error") %>
      prohibited this record from being saved</h2>
    <p>There were problems with the following fields:</p>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
