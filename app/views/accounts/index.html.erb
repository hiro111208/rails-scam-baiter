<%= navigation_bar %>

<div class="mx-2">
  <p id="notice"><%= notice %></p>

  <h1>Accounts</h1>

  <%= link_to 'New Account', new_account_path %>

  <table class="table">
    <thead>
    <tr>
      <th>User</th>
      <th>Currency</th>
      <th>Balance</th>
      <th>Account type</th>
      <th>Account number</th>
      <th>Iban</th>
      <th></th>
    </tr>
    </thead>

    <tbody>
    <% @accounts.each do |account| %>
      <tr>
        <td>
          <%= User.find(account.user_id).email %>
        </td>
        <td><%= account.currency %></td>
        <td><%= account.balance %></td>
        <td><%= account.account_type %></td>
        <td><%= account.account_number %></td>
        <td><%= account.iban %></td>
        <td>
          <%= link_to 'Show', account, class: "mx-2" %>
          <%= link_to 'Edit', edit_account_path(account), class: "mx-2" %>
          <%= link_to 'Destroy', account, method: :delete, data: { confirm: 'Are you sure?' }, class: "mx-2" %>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>

